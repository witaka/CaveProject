<div><%= @quiz.title %></div>
<div><%= @quiz.points %></div>
<div><%= @quiz.passmark %></div>

<div>
    <%= link_to "Edit", edit_quiz_path(@quiz) %></small>

    <%= link_to(
        "Delete",
        quiz_path(@quiz),
        method: :delete,
        data: {
          confirm: "Do you want to delete quiz?"
        }
    ) %>
</div>